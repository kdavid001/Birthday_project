services:
  - type: cron
    name: birthday-cron-job
    schedule: daily  # Change to your preferred frequency (e.g., @daily, @hourly)
    runtime: python
    repo: https://github.com/YOUR_USERNAME/YOUR_REPO_NAME  # Replace with your GitHub repository link
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py